{
    "_comment": "Sanitized OpenClaw configuration. Real secrets are stored in environment variables.",
    "gateway": {
      "mode": "local",
      "auth": {
        "token": "${OPENCLAW_GATEWAY_TOKEN}"
      }
    },
    "agents": {
      "defaults": {
        "workspace": "."
      }
    }
  }